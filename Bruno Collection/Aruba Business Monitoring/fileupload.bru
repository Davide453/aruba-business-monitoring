meta {
  name: fileupload
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/files/upload
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  csv: @file(test_1.csv) @contentType(multipart/form-data)
}

settings {
  encodeUrl: true
  timeout: 0
}
