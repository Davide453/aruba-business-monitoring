meta {
  name: fileupload - errors
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/files/upload
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  csv: @file(test_2.csv) @contentType(multipart/form-data)
}

settings {
  encodeUrl: true
  timeout: 0
}
